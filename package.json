{
  "name": "@emoji-mart/monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "yarn workspace @emoji-mart/website dev",
    "build": "yarn workspace emoji-mart build",
    "build:data": "yarn workspace @emoji-mart/data build",
    "build:react": "yarn workspace @emoji-mart/react build",
    "build:website": "yarn workspace @emoji-mart/website build",
    "prettier": "prettier --check .",
    "prettier:fix": "prettier --write .",
    "test": "yarn node --experimental-vm-modules $(yarn bin jest)",
    "clean": "rm -rf packages/emoji-mart/dist && rm -rf packages/emoji-mart-react/dist && rm -rf packages/emoji-mart-data/dist",
    "vowel:build:emoji-mart": "yarn build && cp -r packages/emoji-mart/* . && cp .gitignore-vowel .gitignore",
    "vowel:build:emoji-mart-react": "yarn build && cp -r packages/emoji-mart-react/* . && cp .gitignore-vowel .gitignore",
    "vowel:build:emoji-mart-data": "yarn build && cp -r packages/emoji-mart-data/* . && cp .gitignore-vowel .gitignore"
  },
  "devDependencies": {
    "@babel/core": "7.18.9",
    "@babel/plugin-transform-modules-commonjs": "7.18.6",
    "jest": "28.0.0",
    "jest-environment-jsdom": "28.0.0",
    "prettier": "2.5.1"
  },
  "prettier": {
    "semi": false,
    "singleQuote": true,
    "trailingComma": "all",
    "arrowParens": "always"
  },
  "engines": {
    "yarn": ">=1"
  }
}
